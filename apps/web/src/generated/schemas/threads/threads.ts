/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * ThreadNest API
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod'

/**
 * @summary List threads in a nest
 */
export const NestThreadListParams = zod.object({
  nestSlug: zod.string()
})

export const nestThreadListQueryLimitDefault = 20
export const nestThreadListQueryLimitMax = 100

export const nestThreadListQuerySortByDefault = `createdAt`

export const NestThreadListQueryParams = zod.object({
  limit: zod.number().min(1).max(nestThreadListQueryLimitMax).default(nestThreadListQueryLimitDefault),
  cursor: zod.string().optional(),
  sortBy: zod.enum(['createdAt', 'updatedAt', 'lastCommentAt', 'score']).default(nestThreadListQuerySortByDefault),
  sortAscending: zod.boolean(),
  search: zod.string().optional()
})

export const NestThreadListResponse = zod.object({
  data: zod.object({
    items: zod.array(zod.object({
      id: zod.string().describe('Thread ID'),
      slug: zod.string().describe('Unique thread slug (within its nest)'),
      title: zod.string().describe('Thread title'),
      createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
      updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
      lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
      commentCount: zod.number().describe('Number of comments on this thread'),
      score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
      viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
      lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
      pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
      author: zod.object({
        id: zod.string().describe('User ID'),
        profile: zod.object({
          username: zod.string().describe('Unique username'),
          displayName: zod.string().nullable().describe('Display name'),
          avatarUrl: zod.string().nullable().describe('Avatar URL')
        }).nullable().describe('Null if the user has no profile'),
        role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
      }).describe('Thread author')
    })),
    meta: zod.object({
      nextCursor: zod.string().nullable().describe('Cursor to fetch the next page, or null if there are no more results'),
      hasMore: zod.boolean().describe('Whether more results are available')
    })
  })
})

/**
 * @summary Create a thread in a nest
 */
export const NestThreadCreateParams = zod.object({
  nestSlug: zod.string()
})

export const nestThreadCreateBodyTitleMin = 3
export const nestThreadCreateBodyTitleMax = 128

export const nestThreadCreateBodyContentMax = 10000

export const NestThreadCreateBody = zod.object({
  title: zod.string().min(nestThreadCreateBodyTitleMin).max(nestThreadCreateBodyTitleMax),
  content: zod.string().min(1).max(nestThreadCreateBodyContentMax)
})

export const NestThreadCreateResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Get a thread by slug
 */
export const NestThreadGetBySlugParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadGetBySlugResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Update a thread's title and/or content
 */
export const NestThreadUpdateParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const nestThreadUpdateBodyTitleMin = 3
export const nestThreadUpdateBodyTitleMax = 128

export const nestThreadUpdateBodyContentMax = 10000

export const NestThreadUpdateBody = zod.object({
  title: zod.string().min(nestThreadUpdateBodyTitleMin).max(nestThreadUpdateBodyTitleMax),
  content: zod.string().min(1).max(nestThreadUpdateBodyContentMax)
})

export const NestThreadUpdateResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Delete a thread
 */
export const NestThreadDeleteParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadDeleteResponse = zod.void()

/**
 * @summary Lock a thread
 */
export const NestThreadLockParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadLockResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Unlock a thread
 */
export const NestThreadUnlockParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadUnlockResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Pin a thread
 */
export const NestThreadPinParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadPinResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Unpin a thread
 */
export const NestThreadUnpinParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadUnpinResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Cast or change a vote on a thread
 */
export const NestThreadVoteParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadVoteBody = zod.object({
  type: zod.enum(['UPVOTE', 'DOWNVOTE'])
})

export const NestThreadVoteResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Remove the current user's vote on a thread
 */
export const NestThreadRemoveVoteParams = zod.object({
  nestSlug: zod.string(),
  threadSlug: zod.string()
})

export const NestThreadRemoveVoteResponse = zod.object({
  data: zod.object({
    id: zod.string().describe('Thread ID'),
    slug: zod.string().describe('Unique thread slug (within its nest)'),
    title: zod.string().describe('Thread title'),
    content: zod.string().nullable().describe('Thread content. Null if the current user cannot read it'),
    createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
    updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
    lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
    commentCount: zod.number().describe('Number of comments on this thread'),
    score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
    viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
    deletedAt: zod.string().nullable().describe('Deletion timestamp, if the thread was deleted'),
    deletedById: zod.string().nullish().describe('ID of the user who deleted the thread. Only present for nest moderators, and never set for platform-removed threads'),
    deletedByPlatform: zod.boolean().optional().describe('Whether the thread was removed by platform moderators rather than nest moderation. Only present for nest moderators'),
    author: zod.object({
      id: zod.string().describe('User ID'),
      profile: zod.object({
        username: zod.string().describe('Unique username'),
        displayName: zod.string().nullable().describe('Display name'),
        avatarUrl: zod.string().nullable().describe('Avatar URL')
      }).nullable().describe('Null if the user has no profile'),
      role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
    }).describe('Thread author'),
    lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
    pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
    access: zod.object({
      isAuthor: zod.boolean().describe('Whether the current user authored this thread'),
      isDeleted: zod.boolean().describe('Whether the thread has been deleted'),
      isLocked: zod.boolean().describe('Whether the thread is locked'),
      isPinned: zod.boolean().describe('Whether the thread is pinned'),
      canViewThread: zod.boolean().describe('Whether the current user can view the thread'),
      canReadContent: zod.boolean().describe('Whether the current user can read the thread\'s content'),
      canEditThread: zod.boolean().describe('Whether the current user can edit the thread'),
      canDeleteThread: zod.boolean().describe('Whether the current user can delete the thread'),
      canCommentThread: zod.boolean().describe('Whether the current user can comment on the thread'),
      canVoteThread: zod.boolean().describe('Whether the current user can vote on the thread'),
      canVoteComment: zod.boolean().describe('Whether the current user can vote on comments in this thread'),
      canModerateContent: zod.boolean().describe('Whether the current user can moderate content on the thread'),
      canManageThreadLock: zod.boolean().describe('Whether the current user can lock\/unlock the thread'),
      canManageThreadPin: zod.boolean().describe('Whether the current user can pin\/unpin the thread')
    }).describe('The current user\'s access and permissions for this thread')
  })
})

/**
 * @summary Search threads by title/content across nests visible to the current user
 */
export const threadSearchQueryLimitDefault = 20
export const threadSearchQueryLimitMax = 100

export const threadSearchQuerySearchMax = 100

export const ThreadSearchQueryParams = zod.object({
  limit: zod.number().min(1).max(threadSearchQueryLimitMax).default(threadSearchQueryLimitDefault),
  cursor: zod.string().optional(),
  search: zod.string().max(threadSearchQuerySearchMax)
})

export const ThreadSearchResponse = zod.object({
  data: zod.object({
    items: zod.array(zod.object({
      id: zod.string().describe('Thread ID'),
      slug: zod.string().describe('Unique thread slug (within its nest)'),
      title: zod.string().describe('Thread title'),
      createdAt: zod.iso.datetime({ offset: true }).describe('Creation timestamp'),
      updatedAt: zod.iso.datetime({ offset: true }).describe('Last update timestamp'),
      lastCommentAt: zod.string().nullable().describe('Timestamp of the last comment on this thread'),
      commentCount: zod.number().describe('Number of comments on this thread'),
      score: zod.number().describe('Net vote score (upvotes minus downvotes)'),
      viewerVote: zod.enum(['UPVOTE', 'DOWNVOTE']).nullable().describe('The current user\'s vote on this thread, if any'),
      lockedAt: zod.string().nullable().describe('When the thread was locked, if it is'),
      pinnedAt: zod.string().nullable().describe('When the thread was pinned, if it is'),
      author: zod.object({
        id: zod.string().describe('User ID'),
        profile: zod.object({
          username: zod.string().describe('Unique username'),
          displayName: zod.string().nullable().describe('Display name'),
          avatarUrl: zod.string().nullable().describe('Avatar URL')
        }).nullable().describe('Null if the user has no profile'),
        role: zod.enum(['OWNER', 'MODERATOR', 'MEMBER']).nullish().describe('The user\'s role in the nest this reference was resolved for. Omitted where role isn\'t resolved for this reference')
      }).describe('Thread author'),
      nest: zod.object({
        name: zod.string().describe('Nest display name'),
        slug: zod.string().describe('Unique nest slug')
      }).describe('The nest this thread belongs to')
    })),
    meta: zod.object({
      nextCursor: zod.string().nullable().describe('Cursor to fetch the next page, or null if there are no more results'),
      hasMore: zod.boolean().describe('Whether more results are available')
    })
  })
})
