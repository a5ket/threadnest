/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * ThreadNest API
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod'

/**
 * Creates a user and starts a session. Tokens are also set as httpOnly cookies.
 * @summary Register a new account
 */
export const authRegisterBodyEmailMax = 320

export const authRegisterBodyPasswordMin = 8
export const authRegisterBodyPasswordMax = 128

export const AuthRegisterBody = zod.object({
  email: zod.email().max(authRegisterBodyEmailMax).describe('The email address to register the account with'),
  password: zod.string().min(authRegisterBodyPasswordMin).max(authRegisterBodyPasswordMax).describe('The account password')
})

export const AuthRegisterResponse = zod.object({
  data: zod.object({
    accessToken: zod.string().describe('Short-lived JWT used to authenticate API requests'),
    refreshToken: zod.string().describe('Long-lived token used to obtain a new access token. Also set as an httpOnly cookie.')
  })
})

/**
 * Starts a session. Tokens are also set as httpOnly cookies.
 * @summary Log in with email and password
 */
export const authLoginBodyEmailMax = 320

export const authLoginBodyPasswordMin = 8
export const authLoginBodyPasswordMax = 128

export const AuthLoginBody = zod.object({
  email: zod.email().max(authLoginBodyEmailMax).describe('The account email address'),
  password: zod.string().min(authLoginBodyPasswordMin).max(authLoginBodyPasswordMax).describe('The account password')
})

export const AuthLoginResponse = zod.object({
  data: zod.object({
    accessToken: zod.string().describe('Short-lived JWT used to authenticate API requests'),
    refreshToken: zod.string().describe('Long-lived token used to obtain a new access token. Also set as an httpOnly cookie.')
  })
})

/**
 * Accepts the refresh token from the request body or the `refresh_token` cookie. Rotated tokens are also set as httpOnly cookies.
 * @summary Rotate the refresh token and issue a new access token
 */
export const authRefreshBodyRefreshTokenMin = 128
export const authRefreshBodyRefreshTokenMax = 128

export const AuthRefreshBody = zod.object({
  refreshToken: zod.string().min(authRefreshBodyRefreshTokenMin).max(authRefreshBodyRefreshTokenMax).optional().describe('The refresh token. Optional when a valid `refresh_token` cookie is present.')
})

export const AuthRefreshResponse = zod.object({
  data: zod.object({
    accessToken: zod.string().describe('Short-lived JWT used to authenticate API requests'),
    refreshToken: zod.string().describe('Long-lived token used to obtain a new access token. Also set as an httpOnly cookie.')
  })
})

/**
 * @summary Log out the current session
 */
export const AuthLogoutResponse = zod.void()

/**
 * @summary Log out all sessions for the current user
 */
export const AuthLogoutAllResponse = zod.void()

/**
 * @summary Request an email verification link for the current user
 */
export const AuthRequestEmailVerificationResponse = zod.void()

/**
 * @summary Confirm an email verification token
 */
export const AuthConfirmEmailVerificationBody = zod.object({
  token: zod.string().describe('The confirmation token sent to the user\'s email address')
})

export const AuthConfirmEmailVerificationResponse = zod.void()

/**
 * Always returns 204, whether or not the email is registered.
 * @summary Request a password reset email
 */
export const authRequestPasswordResetBodyEmailMax = 320

export const AuthRequestPasswordResetBody = zod.object({
  email: zod.email().max(authRequestPasswordResetBodyEmailMax).describe('The email address of the account to request a password reset for')
})

export const AuthRequestPasswordResetResponse = zod.void()

/**
 * @summary Confirm a password reset token and set a new password
 */
export const authConfirmPasswordResetBodyPasswordMin = 8
export const authConfirmPasswordResetBodyPasswordMax = 128

export const AuthConfirmPasswordResetBody = zod.object({
  token: zod.string().describe('The password reset confirmation token sent to the user\'s email address'),
  password: zod.string().min(authConfirmPasswordResetBodyPasswordMin).max(authConfirmPasswordResetBodyPasswordMax).describe('The new account password')
})

export const AuthConfirmPasswordResetResponse = zod.void()

/**
 * @summary Confirm a pending email change token
 */
export const AuthConfirmEmailChangeBody = zod.object({
  token: zod.string().describe('The confirmation token sent to the user\'s email address')
})

export const AuthConfirmEmailChangeResponse = zod.void()
