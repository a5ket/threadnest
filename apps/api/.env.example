PORT=3000

DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

JWT_ACCESS_SECRET="your-access-secret"
JWT_REFRESH_SECRET="your-refresh-secret"

REDIS_HOST="localhost"
REDIS_PORT=6379

WEB_APP_URL="http://localhost:3001"

SMTP_HOST="localhost"
SMTP_PORT=1025
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@example.com"

# S3-compatible object storage (defaults target the local MinIO from docker-compose.yml).
# Swap these to point at any S3-compatible provider (AWS S3, Cloudflare R2, Backblaze B2, DigitalOcean Spaces...) later.
STORAGE_ENDPOINT="http://localhost:9000"
STORAGE_REGION="us-east-1"
STORAGE_BUCKET="threadnest"
STORAGE_ACCESS_KEY_ID="minioadmin"
STORAGE_SECRET_ACCESS_KEY="minioadmin"
STORAGE_PUBLIC_URL="http://localhost:9000/threadnest"
STORAGE_FORCE_PATH_STYLE="true"
