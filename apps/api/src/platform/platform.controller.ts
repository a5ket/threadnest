import { Controller } from '@nestjs/common'

/**
 * Empty placeholder — platform routes currently live under their own subdomain controllers
 * (action-log, content, report, role-grant, suspension), not here.
 */
@Controller('platform')
export class PlatformController { }
